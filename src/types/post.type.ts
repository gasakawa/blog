export type PostType = {
  slug: string;
  category: string;
  date: string;
  title: string;
  excerpt: string;
  tags: string;
};

export type Post = {
  slug: string;
  category: string;
  date: string;
  title: string;
  excerpt: string;
  tags: string;
};
